# VoxCPM2 local inference server.
# Needs Python 3.10-3.12 (3.13+ is NOT supported by the voxcpm / torch stack).
voxcpm
gradio>=4.0
soundfile
numpy
hf_transfer  # robust/parallel/resumable HF downloads — stops the ~8GB model stalling at 0%
